# dbs-hackathon-coloredcoins

Requires colorcore to run in the background.

colorcore repo at https://github.com/OpenAssets/colorcore

runs on bitcoin testnet.

modify config.ini for colorcore with the following values.

version-byte=111

asset-version-byte=115

